import React from "react";

function Footer(props) {
    return (
        //Adding class naems to target in CSS
        //Put every aspect into its own container in order to manipulate it. Give it a class name to target it.
        <div className="footer-credits">
            Brought to you by <span className="ro">Ro</span>
        </div>
    ) 
 }
 
 
export default Footer;